# @openloop/dsh-base

OpenLoop 视觉插件族的共享主题包：61 个设计 token（8 套 OCIX 预设 × 明暗）、「OpenLoop Visuals」设置页宿主、以及供 `dsh.client.external` 共享的 client 模块。

- 消费者：panels / html-artifact / show-widget（经 `@openloop/dsh-base/client`）
- 0.3.0 起为正式 DSH bundle（此前为纯库，设置页注册寄生在 declarative）
